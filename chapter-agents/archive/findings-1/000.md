
 [x] cannot source infix words - fixed

 [ ] enforce short circuiting in binary operators, add tests in root
     may require builtin codegen support for short circuiting

 [ ] A bare `import root` fails with
     `General type error: No instance found for Ord::<=`.

 [ ] `1.0 / 0.0` divergence (host-language design, not IEEE)
    - Python pool: raises `float division by zero` (nexus shows
      `Error: run failed / float division by zero / at m1 (py)`).
    - C++ pool: `"inf"`.
    - R pool: `"inf"`.
    - Workaround: use the `Inf` literal instead of dividing by zero, or
      route the arithmetic through a non-Python pool.

 [ ] Negation edge cases
    - `negate 0.0` prints as `-0` in JSON (sign visible textually).
    - Source literal `-0.0` prints as `0` (sign collapsed). Distinction is
      numerical noise per JSON spec but observably inconsistent.
    - Whitespace rules for `-` from features-integers § "Negation" still
      apply.

 [ ] introducing ! as a purely syntax sugar eval notifier

 [ ] for `run`, need to define `stdlib-<lang>` cases and a clean option in
     `run` to import them. this will streamline the repl case

 [ ] semantic for when an effect statement is duplicated. Should this raise an
     error?

 [ ] typechecker errors for constant overflows? Probably reasonable as an
     additional pass?

 [ ] `-0.0` printed sign vs doc "JSON output does not preserve"

        The doc says the binary format preserves the `+0.0` vs `-0.0` bit
        pattern but "the JSON output does not". Numerically true (JSON `-0`
        and `0` are the same number), but the printed text is not uniform:

        ```console
        $ morloc-manager run -- morloc eval -e 'import root-py; negate 0.0'
        -0
        $ morloc-manager run -- morloc eval -e 'import root-py; -0.0'
        0
        ```

        `negate 0.0` prints as `-0` (sign visible in text), while the source
        literal `-0.0` prints as `0` (sign gone). A reader checking the
        distinction empirically will see something inconsistent with the doc's
        "JSON output does not [preserve]" wording. A stronger claim ("the JSON
        numeric form may or may not carry the sign") or an explicit note about
        the literal-`-0.0`-collapsing-to-`0` case would be more accurate.

 [ ] Integer literals in a `Real` context silently produce garbage — **blocker**

    `features-integers.asc :: Negation and unary minus / Negative literals`, code
    block ending line 281, contains:

    ```morloc
    ys :: [Real]
    ys = [-1.5, -2.0e-3, -0xff]
    ```

    `morloc make` accepts this without warning. The `-0xff` element is not
    promoted to a real; the compiler evidently reinterprets the integer's
    bit-pattern as a `double`, producing garbage/NaN at runtime.

    Minimal reproducer (I added the `module` header and split each case into its
    own binding):

    ```morloc
    module main (r1, r2, r3, r4, r5)

    r1 :: Real
    r1 = 255
    r2 :: Real
    r2 = -255
    r3 :: Real
    r3 = 0xff
    r4 :: Real
    r4 = -0xff
    r5 :: Real
    r5 = 0o377
    ```

    Runtime output:

    ```
    >>> r1        # 255 :: Real
    1.25986739689518e-321
    >>> r2        # -255 :: Real
    "nan"
    >>> r3        # 0xff :: Real
    1.25986739689518e-321
    >>> r4        # -0xff :: Real
    "nan"
    >>> r5        # 0o377 :: Real
    1.25986739689518e-321
    ```

    (`255 == 0x00000000000000FF`, and interpreting those 8 bytes as an IEEE 754
    double gives 1.26e-321 — a subnormal. The negative case flips the sign bit,
    giving a NaN. So integer literals are being wire-passed as `Real` without
    conversion.)

    Same result whether or not `-` is present, whether or not the literal is
    hex/octal/decimal. The direct `ys :: [Real]` example from the docs was also
    tested and gave `[-1.5,-0.002,"nan"]`.

    Either (a) `morloc make` should reject integer literals in a `Real` context
    with a type error, or (b) integer literals should be converted to `Real`. It
    currently does neither. The doc's `ys` example is therefore both
    non-executable and misleading — a reader who trusts it will get NaN in their
    list and no diagnostic.

    Also note the JSON output form `"nan"` (a *string*) for a value declared
    `Real` — that's non-portable JSON regardless.
