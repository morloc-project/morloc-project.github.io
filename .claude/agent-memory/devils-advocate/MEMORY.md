# Devil's Advocate Memory

## Design rationale (don't rederive)
- [`?T` as a CLI positional](project_optional_positional_cli_semantics.md) — `?T` is about the VALUE not the slot; `null` is already the CLI's null token except for `?Str`; constraints any "make it optional" fix must respect.
